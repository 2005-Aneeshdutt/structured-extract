{
  "_comment": "High-capacity arm. ~37M trainable params. Hypothesis: at 4k examples this begins to overfit -- watch for val loss bottoming out before epoch 3 while train loss keeps falling. If it wins on val, it wins; but the README must then argue capacity, not just quote the number.",
  "base_model": "Qwen/Qwen2.5-1.5B-Instruct",
  "lora_rank": 32,
  "lora_alpha": 64,
  "lora_dropout": 0.05,
  "max_seq_length": 2048,
  "load_in_4bit": true,
  "learning_rate": 0.0002,
  "num_train_epochs": 3.0,
  "per_device_train_batch_size": 2,
  "gradient_accumulation_steps": 8,
  "warmup_ratio": 0.05,
  "lr_scheduler_type": "cosine",
  "weight_decay": 0.01,
  "max_grad_norm": 0.3,
  "optim": "adamw_8bit",
  "seed": 3407,
  "dataset_path": "data/processed/hf",
  "eval_steps": 50,
  "save_steps": 100,
  "gen_eval_n": 48,
  "run_name": "qwen2.5-1.5b-r32-a64"
}

{
  "_comment": "Low-capacity arm. ~9M trainable params. Hypothesis: sufficient for JSON structure but under-fits the tier-4 normalization fields (salary parsing, null-over-guess). If rank 8 matches rank 16, the honest conclusion is that the task needs less capacity than assumed -- and we ship the smaller adapter.",
  "base_model": "Qwen/Qwen2.5-1.5B-Instruct",
  "lora_rank": 8,
  "lora_alpha": 16,
  "lora_dropout": 0.05,
  "max_seq_length": 2048,
  "load_in_4bit": true,
  "learning_rate": 0.0002,
  "num_train_epochs": 3.0,
  "per_device_train_batch_size": 2,
  "gradient_accumulation_steps": 8,
  "warmup_ratio": 0.05,
  "lr_scheduler_type": "cosine",
  "weight_decay": 0.01,
  "max_grad_norm": 0.3,
  "optim": "adamw_8bit",
  "seed": 3407,
  "dataset_path": "data/processed/hf",
  "eval_steps": 50,
  "save_steps": 100,
  "gen_eval_n": 48,
  "run_name": "qwen2.5-1.5b-r8-a16"
}

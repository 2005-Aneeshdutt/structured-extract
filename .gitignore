# Artifacts -- NEVER commit. This repo lives under OneDrive on the author's box;
# syncing multi-GB weights through OneDrive corrupts them and burns quota.
data/raw/
data/interim/
data/processed/
models/
*.gguf
*.safetensors
*.bin

# Secrets
.env
*.key

# Python
.venv/
venv/
__pycache__/
*.py[cod]
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# Tooling
wandb/
.ipynb_checkpoints/

# Results: charts + tables ARE committed (they are the point of the README).
# Raw per-example prediction dumps are not.
results/raw_predictions/

# Claude Code local permissions (machine-specific, not for the repo)
.claude/settings.local.json
